extends layout

block content
  h1= title
  h4 running on container #{hostname}
  h4 database  #{database}
  h4 database port #{port}

  table
    thead
      tr
        th Juan
        th Jason
    tbody
      tr
        td Rodriguez 
        td Smith
